plugins {
  id 'java-platform'
}

dependencies {
  constraints {
    rootProject.subprojects.each {
      if (!rootProject.unpublishedModules.contains(it.name) && !it.name.endsWith("-example")) {
        api("${it.group}:${it.name}:${project.version}")
      }
    }
  }
}
