database:
  driverClass: org.h2.Driver
  user: sa
  password: sa
  url: jdbc:h2:mem:test;DB_CLOSE_DELAY=-1

server:
  rootPath: "/api/*"
  applicationConnectors:
  - type: http
    port: 0
  adminConnectors:
  - type: http
    port: 0

propertyForOtherBundle: A property from another required configuration that should be ignored in the DbMigration
