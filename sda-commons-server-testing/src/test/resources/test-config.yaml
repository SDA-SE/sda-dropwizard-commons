envReplaced: ${ENV_REPLACEMENT}
envDefault: ${ENV_DEFAULT:-DEFAULT}
envWithDefaultReplaced: ${ENV_REPLACEMENT:-DEFAULT}
envMissing: ${ENV_MISSING}

server:
  applicationConnectors:
  - type: http
    port: 0
  adminConnectors:
  - type: http
    port: 0

optionalConfig: ${OPTIONAL_CONFIG:-
  property1: ${PROPERTY_ONE}
  property2: ${PROPERTY_TWO:-default} }

example: ${FOO_NAME:-bar-${EXAMPLE_SUFFIX}}
